** .NET Core 1.0.0 **

sudo yum update -y
sudo yum install libunwind libicu -y
curl -sSL -o dotnet.tar.gz https://go.microsoft.com/fwlink/?LinkID=809131
sudo mkdir -p /opt/dotnet && sudo tar zxf dotnet.tar.gz -C /opt/dotnet
sudo ln -s /opt/dotnet/dotnet /usr/local/bin
TODO - Save iptables settings
sudo yum install git -y
git config --global user.name "blongacre22"
git config --global user.email "longacre.bruce@gmail.com"
mkdir coreapp
cd coreapp
git clone https://github.com/blongacre22/pinoacres.git
git reset

cd /home/ec2-user/coreapp/pinoacres/src/Pinoacres.BusinessObjects/Constants
* Modify constants with emails and passwords

cd /home/ec2-user/coreapp/pinoacres/src/Pinoacres.MLBExtraBasesService

dotnet restore
dotnet build
nohup dotnet run > /dev/null 2>&1 &

cd /home/ec2-user/coreapp/pinoacres/src/Pinoacres.Web.UI

* Adjust any code not in source control *
sudo iptables -t nat -A PREROUTING -i eth0 -p tcp --dport 80 -j REDIRECT --to-port 5000
dotnet restore
dotnet build
nohup dotnet run > /dev/null 2>&1 &